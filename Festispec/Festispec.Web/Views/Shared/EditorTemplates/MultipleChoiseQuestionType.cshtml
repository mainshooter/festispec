@model Festispec.Web.Models.Questions.Types.MultipleChoiseQuestionType

@{int index = 0; }
@foreach (var q in Model.Details.Choices.Cols)
{
    <input class="survey-question" name="@Model.Variable" required="true" type="radio" value="@index">@q
    <br />
    index++;
}