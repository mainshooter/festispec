@model Festispec.Web.Models.Questions.Types.ClosedQuestionType

@foreach (var q in Model.Details.Choices.Cols)
{
    @Html.RadioButton(Model.Variable, q, new { @class = "survey-question" });@q
    <br />
}