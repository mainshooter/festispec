@model Festispec.Web.Models.Questions.Types.DrawingQuestionType


<div style="display: inline-block; border: 1px solid black; background: url('../../@Model.ImageUrl'); background-size: contain; background-repeat: no-repeat;">
    <canvas id="draw-canvas-@Model.Id" max-width="400" max-height="400"></canvas>
</div>

<script>
    var canvas = new fabric.Canvas('draw-canvas-@Model.Id');
    canvas.isDrawingMode = true;
    canvas.freeDrawingBrush.width = 3;
    canvas.freeDrawingBrush.color = "#ff0000";
    canvasDict.addCanvas(canvas, @Model.Id);
</script>